# 過去に見つけた不具合と判断

同じ失敗を繰り返さないための台帳です。新しい不具合クラスを見つけたら追記します。

## 1. 県道の番号を国道として拾った

- 症状: 長野市に国道 18 号と 372 号の重用が出ました
  - 国道 372 号は京都から兵庫の路線で、長野県は通らない
- 原因: way/31660216 は長野県道 372 号だが、国道 18 号のリレーションに含まれていました
  - リレーション所属で国道と認め、その way の `ref` を国道番号として読んだ
- 対策: `ref` 由来の番号に裏取りを入れました
- 効果: 長野県で 372・390・15・12・29・25・414・6 号のトークンを排除しました
- 検証: 372 号が出力に無いことを断定します

## 2. OSM のタイポを拾った

- 症状: 山梨県の精進ブルーラインに国道 358 号と 368 号の重用が出ました
  - 国道 368 号は三重県の路線
- 原因: way/263470309 の `official_name` が `一般国道368号` でした
  - `ref` は `358`、`nat_name` は `国道358号` で、`official_name` だけが誤り
- 対策: 名称の読み取り元を `name` と `name:ja` に限り、名称由来の番号にも裏取りを入れました
- 検証: 368 号が出力に無いこと、出力路線が裏取り済み番号の部分集合であることを断定します
- 備考: OSM 本体の誤りなので、修正を投稿する価値があります

## 3. リレーション未登録のバイパスが丸ごと欠けた

- 症状: 長野南バイパスなど、開通から十年以上経つバイパスが国道として出ません
- 原因: 判定にリレーション所属を必須にしていました
  - ルートリレーションの整備は way の整備に大きく遅れる
  - 長野南バイパスは 22 本の `trunk` だが、どのリレーションにも入っていない
- 対策: 国道の格で描かれ都道府県道が主張していないものを認める規則を足しました
- 効果: 長野県で 107 アーク、57 km を回収しました
- 検証: 長野南バイパスが 22 アークあり、国道 19 号に指定されていることを断定します

## 4. 2 か月前のデータで作っていた

- 症状: 新しいバイパスが反映されていません
- 原因: 本家 Overpass が応答せず、同期の遅れたミラーに切り替わっていました
  - `overpass.kumi.systems` の基準時刻は 33 日から 56 日前
- 対策: 全ミラーの基準時刻を調べて最も新しいものを選び、7 日より古ければ警告します
- 効果: 画面にデータ基準時刻と区間の最終更新日を表示するようにしました
- 検証: 基準時刻が記録されており 7 日以内であることを断定します

## 5. 旧道を除外しかけた

- 症状: 旧道が 9 本混入していると判断し、除外する修正を入れました
- 原因: 法令と実態の区別を誤りました
  - 旧道は指定解除まで国道であり、地理院地図も国道として描く
  - 除外は地理院地図から遠ざかる方向
- 対策: 除外をやめ、`former` フラグを立てて残すことにしました
- 教訓: 「余分に見える」ものを削る前に、正解の定義がどちらにあるかを確かめます

## 6. 全国化で裏取りが効かなくなりかけた

- 症状: 未然に防ぎました
  - 全国を一度に判定する設計にすると起きるはず
- 原因: 裏取りの強さは保証集合の小ささから来ています
  - way 自身の主張は、同じ範囲の国道リレーションが保証する番号だけ採用する
  - 1 県なら保証集合は数十だが、全国では 459 番すべてに近づく
  - 濾すものが無くなり、1 番と 2 番の不具合がそのまま戻る
- 対策: 取得だけ全国にし、判定は都道府県の bbox の中に閉じたままにしました
  - pbf は 1 度だけ読み、47 地域ぶんのキャッシュに切り分ける
  - `build_routes.py` は今までどおり 1 地域ずつ判定する
- 検証: 各地域の保証集合が 459 の三分の一未満であること
  - あわせて路線ごとの実在範囲を見て、番号が遠くの県に無いことを断定する
  - 372 号が京都〜姫路の外に無いことは、県別の検査では言えない

## 7. network タグの無いリレーションを見落とした

- 症状: 京都府に国道 478 号が 1 本も出ません
  - 京都縦貫自動車道で、way は 18 本ある
  - `name=国道478号`、`ref=478`、`highway=trunk` が揃っている
- 原因: 国道リレーションを `network` が `JP:national` で始まるもので選んでいました
  - 478 号のリレーションには `network` タグが無い
  - 保証集合に 478 が入らず、way 側の主張が裏取りで全部落ちた
- 計測: pbf 全体で 国道N号 という名の route=road リレーションを数えました
  - `JP:national` が 582、`network` タグ無しが 43
  - 43 の大半は旧道・バイパス・支線で、路線自体は他が保証している
  - 他に手掛かりが無い番号は 478 号だけ
- 対策: 名称が 国道N号 のリレーションを、network が無くても国道として採用するようにしました
  - 問 1 規則 b が way に認めている根拠を、リレーションにも認める形
  - 都道府県道の network を持つものは除く
  - Overpass 側の問い合わせも同じ条件に揃えた
- 検証: 478 号が京都府に出ること、その実在範囲が京都府に収まること

## 8. 海の上に実線を 1,390 km 引いていた

- 症状: 鹿児島市から種子島へ、走れそうな実線が 115 km 続きます
- 原因: 航路の判定を `route=ferry` だけに頼っていました
  - way/561255584 は `国道58号(海上区間）` だが `route=ferry` が無い
  - 判定が車道に落ち、実線で描かれた
- 計測: 名称に海上区間などを含むアークを全国で数えました
  - 車道として描いていたもの 20 本、1,390.5 km
  - 航路として描けていたもの 2 本、63.3 km
  - 58・197・224・260・269・279・280・382・384・389・390・499 号が該当
- 対策: 名称に「海上区間」を含むものを航路として扱うようにしました
- 検証: 鹿児島県の 58 号と 224 号、沖縄県の 390 号に航路アークがあること
- 教訓: 既に決めてある方針が、データの偏りで静かに破れることがあります
  - 海上国道を破線で描く決定は前からあった
  - 二県で見えていなかっただけ

## 9. 番号が衝突する別の道を国道にした

- 症状: 国道 2 号が福岡市と京都府北部に現れました
  - 実際は門司から梅田までの路線
  - 北海道道 39 号奥尻島線が国道 39 号になった。旭川〜網走から 292 km 離れている
- 原因: 規則 c は `ref` の数字を信じます。裏取りはこれを止められません
  - 裏取りは「その番号の路線が地域内に無い」ときだけ効く
  - 国道 2 号は同じ地域に実在するので、福岡都市高速 2 号の `ref=2` は素通りする
- 原因その 2: 工事中の例外が primary を裏口から通していました
  - 問 1 は primary を国道の格に入れない。数字だけの `ref` は証明にならないため
  - `construction=primary` はその除外を迂回していた
- 計測: 規則 c で入ったアークを全国で数え、名称と格で分けました
  - 名称に `高速N号` を含むもの 303 本、207.7 km。全件が都市高速の路線番号
  - そのうち国道N号を名乗るものは 0 件
  - `construction=primary` で正しく入っていたのは 5 本、1.6 km だけ
- 対策: 二つを規則に足しました
  - 名称が都市高速の路線を指すなら `ref` を国道番号として読まない
  - 工事中の国道の格から primary を外す
- 検証: 路線ごとの実在範囲が経路の中に収まること
- 却下した案: 裏付けからの距離で切ります
  - 西九州自動車道は本物の国道 497 号だが、分割開通で区間が飛んでいる
  - 2 km で切ると 332 本 124 km を落とし、拾えたのは 55 本
  - 計測が支持しなかったので入れていない

## 10. タイルの深さを二箇所に書いた

- 症状: z12 より深く寄ると、点線国道も工事中も海上国道も描かれなくなりました
  - 式検証は全て通り、コンソールにも何も出なかった
- 原因: アーカイブが持つズーム範囲を、スタイル側でも `maxzoom: 14` と書きました
  - 実際に切ったのは z12 まで
  - MapLibre は無い z13・z14 を要求し、何も返らないので何も描かない
- 対策: スタイルからズーム範囲を消し、アーカイブの申告だけを使うことにしました
- 検証: ベクタソースが `minzoom`・`maxzoom` を書いていないことを断定します
- 教訓: 11 番と同じ形です
  - 同じ問いに二度答えると、片方が暗黙のうちに古くなる

## 11. 検証スクリプトが役に立っていなかった

- 症状: 式の検証は全て通るのに、ブラウザではレイヤが 1 つも描かれませんでした
- 原因: 検証スクリプトが本物の式を書き写した複製を検査していました
  - 複製は通り、本物は MapLibre に拒否された
  - `zoom` の interpolate を算術式で包んでいた
  - `line-dasharray` をデータ駆動にしていた
- 対策: 検証スクリプトが本物のスタイル定義を読み込んで検査するようにしました
- 教訓: 検証対象を書き写した時点で、検証は検証でなくなります

## 12. 海の上の実線がまだ残っていた

- 症状: 8 番を直したあとも、有明海・東京湾・明石海峡・伊勢湾に実線が残っていました
  - 三角から島原へ 26 km と 22 km の直線が車道として出ていた
- 原因: 海上区間と述べている欄を名称に決め打ちしていました
  - way/673930217 は `name=国道324号`、`description=海上区間`
  - 判定は `name` と `name:ja` しか読んでいなかった
- 計測: pbf 全体で「海上区間」と書いた way を数えました
  - 34 本。名称に書いたもの 20 本、`description`・`note` だけのもの 14 本
  - 後者は言い換えではなく別の路線
  - 名称だけでは 16・28・30・42・57・259・317・324 号を落とす
- 対策: 読み取り元に `description` と `note` を足しました
  - `TAGS_USED` も広げる。広げないとキャッシュにその欄が載らない
- 検証: 千葉 16、愛知 42・259、兵庫 28、香川 30、愛媛 317、熊本 57・324 に航路アークがあることを断定します
- 教訓: 8 番と同じ形です。証拠のある欄は一つとは限りません

## 13. 画面側が重用を丸めていた

- 症状: 4 重用の区間をクリックすると、ポップアップが国道 202 号の単独指定と述べました
  - 番号を丸めないことがこの地図の存在理由。その画面が丸めていた
- 原因: 同じ画素の下に複数のアークがあります
  - 交差点、並走、立体交差では珍しくない
  - `queryRenderedFeatures` が返す順はタイルの順で、深さとは無関係
  - その先頭を採用していた
- 計測: n が 3 以上のアーク 25 本の上で 75 点を抽出しました
  - 5 点で先頭が最も浅いアーク
- 対策: 描画と選択の両方を深さの順にしました
  - `line-sort-key` に `n` を渡し、重用を単独指定の上に描く
  - ポップアップは画素の下で最も深いアークを説明する。上に描かれている線と一致する
- 検証: 同じ抽出で丸めが 0 点になること
- 教訓: 目的はデータだけでなく画面側でも破れます

## 14. 未開通区間を実線で描いていた

- 症状: 国道 360 号(白山白川郷ホワイトロードが代替路)の 16.4 km、国道 274 号の
  16.1 km(`description=国道274号不通区間`)など、道の無い区間が実線で出ていました
- 原因: `classify()` が `highway=planned`・`proposed` を場合分けせず `road` に落としていました
  - リレーションの連続性を保つために引かれた直線で、走れる道ではない
- 計測: 全国で 86 アーク、173.3 km。`proposed` が 76、`planned` が 10
  - 最長は国道 360 号の 16.4 km、次いで国道 274 号の 16.1 km
- 対策: `kind` に `unopened` を足し、既存の「点線国道・工事中」の表示切り替えに含めました
  - 破線は 4 種のうち最も細かくした。徒歩道より実体が薄いため
- 落とし穴: 「海上区間」と書いた 34 本のうち 32 本は `highway=planned` を持ちます
  (8 番・12 番)。`highway` の判定をサーチ順で先に置くと、直したばかりの海上国道が
  未開通で上書きされます。`classify()` では海上区間の判定を常に先に置きます
- 検証: 北海道 274、石川 360、新潟 353、神奈川 357、三重 1 に `unopened` アークが
  あることを断定します
- 教訓: 8 番・12 番と同じ形です。「走れないものを実線で引く」不具合は海だけでなく
  陸にもあります

## 15. 高速道路番号が付いた国道が丸ごと消えていた

- 症状: 国道 2 号(第二神明道路、兵庫県明石市)と国道 28 号(神戸淡路鳴門自動車道)が
  地理院地図では国道として描かれているのに、国道マップには 1 本も出ていませんでした
- 原因: `extract_pbf.py` の候補判定が `ref` タグ全体に対して「数字とセミコロンだけ」
  を要求する正規表現(`REF_NUMERIC`)を使っていました
  - 第二神明道路は `ref=E93;2`、神戸淡路鳴門自動車道は `ref=E28;28` で、高速道路
    としての路線番号(E ナンバー)と国道番号がセミコロンで同居している
  - 全体一致の正規表現はこの形を丸ごと拒否した
  - この 2 本はどちらも国道リレーションのメンバーでもなく(第二神明道路の 1 本だけ
    が偶然別のバイパスリレーションに属していた)、リレーション経由の救済も働かな
    かった。候補にすら挙がらないので、`build_routes.py` の裏取りに届く前に消えて
    いた
  - 有料道路かどうかは無関係。同じ書式を持つ他の高速道路国道(播但連絡道路
    など)は国道リレーションのメンバーだったので影響を受けていなかった
- 対策: `build_routes.py` の `tokens()` はセミコロン区切りをトークンごとに見ており
  `E93;2` からでも `2` を正しく拾えます。`extract_pbf.py` 側の判定をこの関数の再利用
  に置き換え、独自の正規表現を重複して持たないようにしました
- 効果(兵庫県): 国道 2 号 311.5 km/857 アーク → 362.7 km/971 アーク、国道 28 号は路線
  一覧に無かった状態から 307.5 km/640 アークで登場
- 検証: 第二神明道路・神戸淡路鳴門自動車道が兵庫県に存在し、それぞれ国道 2 号・
  28 号に指定されていることを断定します(長野南バイパスと同じ形、3 番を参照)

## 16. 高速道路としての国道を種別として分けた

- 経緯: 15 番の調査で、E ナンバーが付くような高速道路としての国道は、ふつうの
  平面国道と利用者から見て別物だという指摘がありました
- 対応: `kind=expressway` を新設し、表示・非表示を切り替えられるようにしました
- 判定: `ref` の書式ではなく `highway=motorway` で行います
  - 全国で計測すると、高速道路番号と `motorway` が両方揃うもの 6,321 本に対し、
    `motorway` だが番号の頭文字が違うもの(`C3`・`A1` など)が 6,619 本ある
  - `ref` の書式で判定すると後者を取りこぼす
  - 番号だけあって `motorway` でないもの 199 本は全て `construction=motorway`
    (建設中)で、`construction` の判定が先に効くので問題にならない
  - 理由の全文は RULES.md の「自動車専用道路」を参照
- 検証: 兵庫県の国道 2 号・28 号、徳島県の国道 28 号に `expressway` アークがある
  ことを断定します。ブラウザでの実描画確認では、自動車専用道路レイヤーが単独で
  表示・非表示を切り替えられることを確認しました

## 17. 建物のポリゴンが国道として登録されていた

- 症状: 国道 122 号(埼玉県川口市)に、バイク用品店「レッドバロン川口南」の名前
  で店舗一点が混ざっていました。千葉・茨城・埼玉・東京の 4 都県すべてに同じ
  way が出ていました(bbox が重なる範囲全部で判定されるため)
- 原因: way/1010859005(店舗の建物ポリゴン。`building=retail`、`highway`
  タグなし)が、OSM 上で国道 122 号のルートリレーションに、道路の way と区別
  なくメンバーとして追加されていました
  - 規則 a は「そのリレーションが番号を持てば国道と認める」としか判定して
    おらず、メンバーが `highway` タグを持つ道路かどうかは確認していなかった
- 計測: 全国で、国道リレーションのメンバーのうち `highway` タグを持たない
  もの 124 件を洗い出しました
  - 大半(107 件)は `国道352号` のように名称に路線名を持つ、実在する道路の
    アーク(`highway` タグの記入漏れ)で、除外してはいけない
  - 始点と終点が同じノードの閉じた形(ポリゴン)のものは 8 件のみで、内訳は
    レッドバロン川口南・藤沢市民会館・ペットショップ ワンラブ(いずれも建物)
    と、トンネル・橋の外形線が別に記録された 4 件だった
- 対策: `highway` タグも `route=ferry` タグも持たない、始点と終点が同じ閉じた
  way を候補から除外しました。`route=ferry` は除外の対象から外しています。
  海上区間のフェリー航路は正当に `highway` タグを持ちません(RULES.md 海上区間)
- 検証: 千葉・茨城・埼玉・東京の出力にレッドバロン川口南・藤沢市民会館・
  ペットショップ ワンラブが含まれないことを断定します
- 教訓: 規則 a は「リレーションに入っているか」しか見ておらず、「入っている
  ものが道路か」は別の問いでした

## 18. 県道のリレーションに乗っているというだけで国道から除外・混入していた

- 症状: 広島県の国道 2 号から広島南道路が丸ごと抜け落ち、断片的な短い区間だけが
  飛地のように残りました。茨城県では国道 355 号旧道(大洗友部線)に、無関係な
  国道 16 号との重用が誤って付きました
- 原因: 都道府県道の負の証拠(`prefectural_way_ids`)が、番号を持たない
  単なる way ID の集合でした
  - 規則 c は「都道府県道のリレーションが主張していない」ことを求めていたが、
    実装は「その way がどれか 1 つでも県道のリレーションに乗っているか」しか
    見ておらず、乗っている県道の番号が way 自身の主張する番号と一致するかは
    確認していなかった
  - 広島南道路(`ref=2`)は広島県道 243 号広島港線(`ref=243`)にも古い
    メンバーシップが残っており、無関係な番号でも規則 c から丸ごと弾かれていた
  - 大洗友部線(`ref=16`)は茨城県道 16 号大洗友部線(`ref=16`)のメンバーで、
    番号が一致するので本来弾かれるべきだったが、そのガードは規則 c(そもそも
    国道か)にしか掛かっておらず、「何号か」を決める `ref` 由来の和集合の
    計算では素通りしていた。国道 355 号旧道のリレーション経由で先に国道と
    認められたあと、16 号が unguarded なまま紛れ込んだ
- 計測: 全国で、旧方式なら「県道リレーションに乗っている」というだけで
  余剰番号を疑われていた way は 76 件
  - そのうち 10 件は、乗っている県道が同じ番号を主張しており、誤混入として
    正しく削除される
  - 残り 66 件は、乗っている県道の番号が way 自身の番号と無関係で、実際には
    複数の国道が重複指定された正当な区間だった(兵庫県境の国道 9 号が
    29・53・373 号とも重複する区間、広島県の巴橋が国道 375・433・434 号を兼ねる
    区間など)。番号を見ずに一律除外していたら、この 66 件を壊すところだった
  - 規則 c 側では、県道リレーションに乗っているというだけで丸ごと弾かれていた
    relation-less な国道候補が全国で 307 アーク見つかりました(秋田県の秋田
    自動車道鷹巣西道路・鷹巣大館道路など、県道の格を持つ道が高速道路として
    国道 7 号のバイパスも兼ねる区間が目立つ)
- 対策: `fetch_osm.py`・`extract_pbf.py` の県道クエリを、way ID の集合ではなく
  タグと members を持つリレーション本体を取るように変えました。
  `resolve_prefectural_claims()` で「way → 県道が主張する番号」の対応を作り、
  規則 c と `ref` 由来の和集合の両方で、way 自身が主張する番号のうち県道が
  同じ番号で主張しているものだけを除きます
- 検証: 広島県で広島南道路が国道 2 号として出ること、大洗友部線が国道 355 号のみ
  を持つこと、兵庫県の国道 9 号・広島県の国道 375 号の重複指定が壊れていないこと
  を断定します
- 教訓: 「県道のリレーションに乗っている」ことと「県道として同じ番号を名乗って
  いる」ことは別の情報でした。前者だけで判定すると、番号が無関係でも道連れに
  してしまいます

## 19. 一般車両が通れない生活道路に国道の名前だけが付いていた

- 症状: 福島県内陸部(`?routes=6#16.35/37.465705/140.946905`)に国道 6 号との
  重用が出ましたが、実際の国道 6 号は太平洋側(水戸〜仙台)を通る路線で、この
  地点は大きく外れています。地理院地図ではただの市道です
- 原因: way/497559205 は `highway=residential`、`access=no`、
  `motor_vehicle=no`、`name=国道6号` というタグを持っていました
  - 規則 b は名称が `国道N号` であることだけを国道と認める根拠にしており、
    highway の格や通行可否は問わない
  - 裏取りは「その番号の国道が同じ県内に実在するか」だけを見るため、国道 6 号が
    福島県浜通りに実在する以上、この無関係な生活道路も通過してしまう
- 計測: 全国で規則 b のみ(リレーションに拠らず名前だけ)で採用されている
  way は 10,810 件あり、`highway=residential` はそのうち 33 件でした
  - 33 件中 32 件は正当な旧道・側道、または一般車両が通行できる本物の国道
    区間(`国道322号`・`国道313号`・`国道324号` など、いずれも `ref` を伴う)
  - `highway=residential` かつ `access=no` かつ `motor_vehicle=no` の 3 条件
    が揃うのは全国でこの 1 件だけだった
  - 参考として `access=no` を持つ「国道N号」名の way 全体(200 件)も確認し
    ました。大半は `highway=trunk` の酷道(国道 488 号・152 号・418 号など)で、
    3 条件には該当しません。`highway` の格を見ずに `access=no` だけで弾く案は
    この時点で却下しています
- 対策: 規則 b に、`highway=residential` かつ `access=no` かつ
  `motor_vehicle=no` の way(リレーションによる裏付けが無い場合に限る)を
  除外する例外を追加しました
- 検証: 福島県の出力に way/497559205 が含まれないことを断定します
- 教訓: 9 番と同じ形です。名称由来の裏取りも「同じ番号の国道が同じ県内に実在
  するか」しか見ておらず、実態(道の格・通行可否)とは無関係でした

## 20. 名前に番号を持たない都市高速が国道番号を横取りした

- 症状: 東京都の国道 1 号に、首都高速都心環状線の 1 km に満たない区間が重用と
  して出ました。都心環状線は首都高速道路の路線であって国道 1 号(日本橋〜大阪)
  ではありません
- 原因: way/560259106・1219615735(`highway=motorway`、`ref=1`、
  `name=首都高速都心環状線`)は 9 番の対策(`高速N号` という名前を国道番号と
  読まない)をすり抜けました
  - 9 番のガードは way 自身の名前に `高速N号` という文字列を要求します
  - 首都高速 1 号羽田線のように名前に番号があれば止まりますが、都心環状線は
    路線としては同じ「首都高速1号線」の一部でありながら、名前に数字を持たない
  - この way は relation/4256244(`name=首都高速1号羽田線`、
    `network=首都高速道路`、`ref=1`)のメンバーで、リレーション側には番号が
    ちゃんと乗っている。9 番のガードは way の名前しか見ておらず、この
    リレーションの存在を使っていなかった
- 計測: pbf 全体で、route=road の relation のうち `network` が空でなく
  `JP:` で始まらないもの(首都高速道路・阪神高速道路・名古屋高速道路など、
  行政区画ではなく事業者名を name space に使っている)を数えました
  - 34 件。うち 33 件は `ref` が実在の国道番号と衝突します(1〜11 号が
    首都高速、33〜46 号あたりが阪神高速の系列)
  - 衝突する 33 件の下に member way が 1,009 本ありました
- 対策: 18 番で作った「都道府県道が同じ番号を主張していれば ref 由来の
  番号を信じない」仕組み(`prefectural_relations`/`resolve_prefectural_claims`)
  を、`都道府県道` に限らない形に広げました
  - 選別条件を「`network` が `JP:prefectural` で始まる」から「`network` が
    `JP:prefectural` で始まる、または `network` が空でなく `JP:` で
    始まらない」に広げ、`prefectural_relations`/`resolve_prefectural_claims`
    は `competing_relations`/`resolve_competing_claims` に改名しました
  - 都道府県道も都市高速も「別の主体が同じ書式で自分の番号を名乗っている」
    という同じ形の証拠だったので、名前を実態に合わせました
  - `fetch_osm.py` の Overpass クエリも同じ条件に揃えています
- 検証: 東京都の出力に首都高速都心環状線が国道 1 号として出ないことを断定
  します
- 教訓: 9 番のガードは「way の名前」という一つの経路しか見ていませんでした。
  同じ実態がリレーションの `network` という別の経路でも表れることがあり、
  7 番・12 番と同じ「証拠のある欄は一つとは限らない」形です

## 21. ハイキング道になった旧道が現行リレーションに乗ったままだった

- 症状: 長野・静岡県境の 19 m の橋(way/152895667)が、実線の国道 152 号として
  出ました。現地は `highway=residential`、`route=hiking`、`tourism=yes` で、
  地図アプリで言えばハイキングコースです
- 原因: この way は `name` を持たず、`old_name=国道152号`、
  `historic:highway=trunk`、`historic:ref=152` という OSM の「かつてこの格の
  道路だった」という書き方をしていました
  - `is_former()` は way 自身の `name`・`name:ja` に「旧道」「廃道」
    「旧国道」の文字列があるかしか見ておらず、`historic:highway` という
    別の書き方には反応しない
  - しかもこの way は今も現行の「国道152号」本線リレーション(旧道用の
    別リレーションではない)のメンバーのままなので、規則 a はそのまま
    通す。旧道と気づく手掛かりが `former` 判定側にしか無かった
- 計測: 全国で、国道リレーションが保証する way のうち `historic:highway`
  を持ち、かつ名前に旧道の語を含まないものを数えました。1 件(この way)
  だけでした
- 対策: `is_former()` に `historic:highway` の有無を足しました。TAGS_USED
  にも `historic:highway` を追加しています
- 検証: `verify.py` の「旧道を名乗る way はすべて former を持つ」検査を、
  名前ベースで former を再構成する等価検査から、名前ベースの旧道が
  `former` の部分集合であることを見る検査に直しました。出力の `name` だけ
  からは `historic:highway` 由来の former を再現できないためです
- 教訓: 5 番の「旧道は除外せず former で残す」という判断自体は正しいまま
  でしたが、former と気づく手段が「名前の文字列」の一種類しかありませんでした。
  同じ実態を述べる OSM の書き方は複数あります

## 22. highway を持たない計画路線が実線で出ていた

- 症状: 岐阜県の国道 257 号に、峠を越える 0.8 km の区間が実線で出ました。
  トンネルも道路も航空写真に存在しません
- 原因: way/743758644 は `highway` タグを持たず、`proposed=trunk`・
  `layer=-5` だけを持っていました。「造ればこの格になる」という意味の、
  `highway=planned`・`proposed`(14 番で対策済み)とは別の書き方です
  - `classify()` の未開通判定は `highway` の値だけを見ており、`highway`
    キー自体が無い場合は素通りして `road`(実線)になる
  - この way は現行の「国道257号」本線リレーションのメンバーなので、
    規則 a で採用はされる。未開通と気づく手掛かりが分類側に無かった
- 計測: 全国で、国道リレーションが保証する way のうち `highway` を持たず
  `proposed`・`planned` のどちらかを持つものを数えました。1 件(この way)
  だけでした
- 対策: `classify()` に、`highway` が無く `proposed`・`planned` のどちらかを
  持つ場合を `unopened` とする分岐を足しました。TAGS_USED にも
  `proposed`・`planned` を追加しています
- 検証: 岐阜県の出力で way/743758644 の `kind` が `unopened` であることを
  断定します
- 教訓: 14 番と同じ形です。「まだ無い」という同じ実態を、OSM は
  `highway=planned` と `proposed=trunk`(`highway` 抜き)の両方で書きます

## 23. 孤立した trunk 級の番号衝突は直せなかった

- 症状: 奈良・大阪府境(way/1155425600、`highway=trunk`、`ref=170`、
  `name=大県本郷線`)が国道 170 号として出ました。実際の国道 170 号(大阪外環状線)
  は大阪府内で完結する路線で、この地点まで届きません
- 調査: この way はどのリレーションにも属していません。周辺には同じ形の
  相互接続した 6 本の関連 way(計 0.4 km)があるだけで、国道 170 号の本線
  (リレーション由来、奈良・大阪で計 400 本以上)とは 1 か所もノードを
  共有していません
- 却下した案: 「relation 由来の同じ番号の網につながっていない tag 由来の
  way は落とす」という接続性の検査を検討しましたが、実装しませんでした
  - 9 番の「却下した案」で、裏付けからの距離で切る案がすでに測定・却下
    されています。西九州自動車道(本物の国道 497 号)は分割開通のため
    2 km 以上離れた区間が多く、2 km で切ると 332 本 124 km を失い、
    拾えたのは 55 本でした
  - 接続性の検査は形が違うだけで同じ副作用を持ちます。分割開通した
    本物のバイパス(長野南バイパスなど、規則 c が存在する理由そのもの)は
    往々にして本線とノードを共有しないため、同じだけ壊れます
  - 大県本郷線が誤りだと分かるのは、この way だけを見てではなく、
    「国道 170 号は大阪府内で完結する」という地理的な事実を知っているから
    です。それは way のタグにもリレーションにも書かれていません
- 現状: 規則 c(trunk 級かつ競合リレーション無し)が RULES.md に書いてある
  形のまま残る限り、この種の孤立した番号衝突は原理的に取りこぼします。
  20 番までの修正はどれも「別の主体の番号だとリレーションか名前のどちらかに
  書いてある」場合を捕まえるもので、大県本郷線のようにどこにも書かれて
  いない場合は対象外です
- 教訓: 直せる不具合と直せない不具合を区別しました。way 単体のタグからは
  判断できません。道路統計年報との突き合わせは実装しましたが(TRIAGE.md、
  `compare_annual_report.py`)、全国計しか出ないので、この種の誤りは
  1,297 km の「説明できていない残り」に紛れます。路線別・都道府県別に
  比べられれば定量化できる可能性があります

## 24. 地理院地図の 6 重複表示を疑ったが、地理院地図側の誤りだった

- 発端: 北海道網走市新町、石北本線と網走川に沿う区間で、地理院地図は
  国道 39・238・239・240・242・243 号の 6 重複を示すが、国道マップは
  way/34303546(南中央通)を含む区間を国道 39・240・242・243 号の
  4 重複としてしか出さないという報告を受けました
- 最初に疑った説: OSM 本体がこの区間に 238 号・239 号の指定を欠いている
  (国道マップ側のデータ欠落)という仮説を立て、いったんはそう記録し
  かけました
  - way/34303546・294458946・38997273(南中央通・北中央通)は
    `ref=39;240;242;243` のみで、国道 238 号(relation/6621211)・
    239 号(relation/6612481)のメンバーでもない。ライブの OSM API
    でも同じ内容を確認済みで、キャッシュの古さによる見かけ上の差では
    なかった
  - しかしこの仮説は、地理院地図のスクリーンショットに写る 6 つの標識の
    並びだけを根拠にしており、ズームレベル 16 台では画面中心の座標と
    実際の標識の位置が数百 m 単位でずれることを見落としていました
- 裏取りで判明した事実: 北海道開発局網走開発建設部が公開する「道路台帳」
  (路線の管理者自身が作る一次資料)を確認すると、国道 238 号は全長
  165,860 m のうち重複延長がわずか 27 m でした
  - 重複延長は「他の路線の道路法第 11 条が適用される区間」の延長で、
    238 号が 39・240・243 号(いずれも 238 より若い番号)と網走市内で
    1 km 超にわたって重複しているなら、この数字はもっと大きくなるはず
  - 238 号の主要な経過地は「網走市、北見市、佐呂間町、湧別町」で、
    起点からすぐ内陸(北見・佐呂間方面)へ向かうと読め、OSM が実際に
    持っている経路(大曲 1 交差点から湧別・紋別方面、way/104675143 以下)
    と一致する
  - Wikipedia の 39・240・242・243 号の重複区間表(4 記事とも独立に確認
    済み)は、大曲 1 交差点を境に「39・240・242・243 号は市街地方向」
    「238・239・242 号は湧別・紋別方向」ときれいに二分されており、
    6 路線が同時に重なる区間は記載が無い
  - 現地のストリートビューでも該当区間の標識は 39 号のみで、238・239 号
    の標識は確認できなかった
- 結論: 国道マップ・OSM は正しく、地理院地図がこの区間に 238・239 号を
  誤って表示していました。OSM 編集は不要です
- 参照(2026-08-23 時点、いずれも今後変わりうる)
  - 道路台帳 238 号: `https://www.hkd.mlit.go.jp/ab/koubutu/bkh911000000b77z-att/bkh911000000b7cr.pdf`
    (第一表の重複延長の欄)
  - 道路台帳 239 号: `https://www.hkd.mlit.go.jp/ab/koubutu/bkh911000000b77z-att/bkh911000000b7dx.pdf`
    (同、実延長・重複延長の欄)
  - Wikipedia 国道 39・238・239・240・242・243 号、各記事の「重複区間」節
  - OSM: `way/34303546`・`way/294458946`・`way/38997273` の `ref` と所属リレーション
    - `https://api.openstreetmap.org/api/0.6/way/34303546.json` と `/way/34303546/relations.json` で当日確認
  - ストリートビュー: 依頼者が大曲 1 交差点〜新町付近で個別に確認したもの
    - 座標と撮影日は記録していない
- 教訓: 地理院地図は行政の一次情報だが無謬ではありません
  - スクリーンショットの標識の並びだけで「地理院地図が正しく国道マップが間違っている」と判断しなかった
  - 路線管理者自身の一次資料(道路台帳、重複延長の合計値)まで遡って裏取りした
  - 誤った修正、つまり OSM への誤った編集を未然に防げた

## 25. former 孤立検出が水増しされていた

- 症状: `compare_n13.py` の孤立候補(issue #9)は延べ 3,546 件でしたが、実データで測り直すと大半が過検出でした
- 原因 1: 重複排除をしていませんでした
  - 隣接県の bbox が重なるため、同じ way が複数県の一覧に重複して出ます
  - way id で数え直すと 1,644 本で、うち 1,301 本が 2 県以上に重複していました
- 原因 2: 判定がアークの中点 1 点と N13 の距離だけでした
  - 現行判定の候補 804 件のうち 45 件は、アークの半分以上が実は N13 国道線に覆われていました
  - 旧道は現道と両端で接続するため、両端の頂点だけを見る判定はどうしても一致してしまいます
- 原因 3: N13 の読み込みを地域の bbox で切っていました
  - 県境際のアークは、真横に N13 国道線があっても bbox の外に落ちます
    - 旧判定(中点 1 点、bbox で切った N13)では「N13 なし」と誤判定されていた
  - この bbox 起因の誤判定候補を全国の N13 で測り直すと、該当は 0 件でした
- 対策
  - `load_kokudo_raw` を追加し、孤立方向はメッシュ全体(bbox で切らない)の N13 を使うようにしました
  - `coverage_ratio` を追加し、アークを 50 m 間隔で再標本化して、100 m 以内に N13 がある点の割合で判定するようにしました
  - `cluster_by_endpoint` として端点共有の DSU を切り出し、`cluster_gaps` と孤立側の新設 `cluster_former_arcs` の双方から使うようにしました
  - `region all` を追加し、全 47 都道府県の former アークを way id で重複排除してから判定するようにしました
- 計測: 全国で実行すると、被覆率 0% が 501 本、0〜20% が 107 本、20〜80% が 298 本、80〜100% が 738 本でした
  - issue #27 の時点の数字は 501・104・293・746 で、OSM・N13 双方の更新による差分の範囲内です
  - 被覆率 20% 未満の 608 本をクラスタにまとめると 206 件でした
    - issue #27 の見積りは 209 件
- 原因 4(CodeRabbit のレビューで判明): 被覆率 0% を「N13 なし」と同一視していました
  - 50 m 間隔のサンプルは、サンプルとサンプルの間にある N13 を見落とせます
    - どのサンプルからも 100 m 超だが、N13 が 1 本も無いわけではない状況が被覆率 0% に含まれる
    - 両端で現道に接続する旧道が頂点だけの判定では一致してしまう(原因 2)のと対になる、サンプル方式そのものの見落とし
  - 対策として `coverage_ratio` の戻り値に `min_dist` を足しました
    - `min_dist` は全サンプル中の最小距離。1 件も見つからなければ `None`
    - `min_dist is None` のときだけ「N13 なし」と表示する
    - 被覆率 0% で `min_dist` があるクラスタは「被覆率 0%(最寄 N13 X m)」と表示する
  - この修正後、全国で 206 クラスタ中「N13 なし」表示になったのは 0 件でした
    - 109 クラスタは被覆率 0% でも最寄 N13 の距離が付く
    - 原因 3 の bbox 起因の誤判定と合わせ、「本当に周囲に N13 が無い」候補は今のところ実在しない
- 却下した案: 全アークを先にクラスタ化し、クラスタの合計被覆率で 20% 未満を選ぶ案
  - gap 方向は不一致と判定した N13 レコードだけをクラスタ化しており、一致したレコードは対象に入れません
  - 孤立側も同じ順序(先に候補を選び、候補だけをクラスタ化)にする方が対称です
  - 先にクラスタ化する案では 125 クラスタにしかならず、issue #27 の実測値 209 件と大きく外れました
- 検証: `mise run compare-n13 all` を実行し、上記の分布とクラスタ数を確認します
